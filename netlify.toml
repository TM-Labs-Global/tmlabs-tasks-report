# Netlify configuration file
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Disable secrets scanning to prevent false-positives with the word "resend" (which matches SMTP_USER)
  SECRETS_SCAN_ENABLED = "false"
